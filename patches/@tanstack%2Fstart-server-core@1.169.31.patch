diff --git a/src/createStartHandler.ts b/src/createStartHandler.ts
index f4acaed..5243a5f 100644
--- a/src/createStartHandler.ts
+++ b/src/createStartHandler.ts
@@ -779,6 +779,15 @@ export function createStartHandler<TRegister = Register>(
       request.signal.throwIfAborted()
       responseOwnsCleanup = response.serverSsrCleanup === 'stream'
       return response.response
+    } catch (err) {
+      // A disconnected client is expected and must not become an unhandled 500.
+      if (request.signal.aborted && err === request.signal.reason) {
+        return new Response(null, {
+          status: 499,
+          statusText: 'Client Closed Request',
+        })
+      }
+      throw err
     } finally {
       if (router?.serverSsr && !responseOwnsCleanup) {
         // Clean up router SSR state if it was set up but won't be cleaned up by the callback
diff --git a/dist/esm/createStartHandler.js b/dist/esm/createStartHandler.js
index 7de08da..f8d7941 100644
--- a/dist/esm/createStartHandler.js
+++ b/dist/esm/createStartHandler.js
@@ -411,6 +411,12 @@ function createStartHandler(cbOrOptions) {
 			request.signal.throwIfAborted();
 			responseOwnsCleanup = response.serverSsrCleanup === "stream";
 			return response.response;
+		} catch (err) {
+			if (request.signal.aborted && err === request.signal.reason) return new Response(null, {
+				status: 499,
+				statusText: "Client Closed Request"
+			});
+			throw err;
 		} finally {
 			if (router?.serverSsr && !responseOwnsCleanup) router.serverSsr.cleanup();
 			router = null;